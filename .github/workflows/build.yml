name: Build FloatClock
on: workflow_dispatch

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: build
        run: make all
      - name: upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: FloatClock-app
          path: build/*.app
